# Ukázkový konfigurační soubor (.env.example)
# vytvořte si lokální kopii s názvem .env a upravte ji podle svého prostředí;
# funkce load_dotenv() z knihovny python-dotenv načítá proměnné prostředí ze souboru .env;
# před použitím je nutné mít nainstalovanou knihovnu python-dotenv;

DB_HOST=localhost
DB_USER=test_user
DB_PASSWORD=test_heslo
DB_TEST_NAME=task_manager_test
